{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "panel",
      "runtimeExecutable": "py",
      "runtimeArgs": ["-c", "import sys; sys.path.insert(0, 'src'); from scanner.cli import main; sys.argv = ['scanner', 'serve', '--port', '8001', '--no-auto']; main()"],
      "port": 8001
    }
  ]
}
